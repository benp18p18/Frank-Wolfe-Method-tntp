#ifndef Q_SORT
#define Q_SORT


void q_sort(double *l, int *p, int lo, int hi);

int part_find(double *l, int *p, int lo, int hi);

int reset(double l[], int p[], int lenght); 

int mover(double *l, int *p, int lenght, int point, double point_val); 

#endif
